language: c
compiler: clang

# Asterisk-11
before_script:
 - autoreconf -fi

before_install:
# - sudo apt-add-repository -y ppa:d-shegolihin/asterisk
 - sudo echo "deb http://archive.ubuntu.com/ubuntu trusty main restricted universe multiverse"            >  /etc/apt/sources.list.d/trusty.list
 - sudo echo "deb http://archive.ubuntu.com/ubuntu trusty-updates main restricted universe multiverse"    >> /etc/apt/sources.list.d/trusty.list
 - sudo echo "deb http://security.ubuntu.com/ubuntu/ trusty-security main restricted universe multiverse" >> /etc/apt/sources.list.d/trusty.list
 - sudo echo "deb http://archive.canonical.com/ubuntu/ trusty partner"                                    >> /etc/apt/sources.list.d/trusty.list
 - sudo apt-get update -qq
# - sudo apt-get install -qq asterisk-11.2.1
 - sude apt-get install -qq asterisk asterisk-dev asterisk-config asterisk-modules

notifications:
  email:
    recipients:
      - ddegroot@talon.nl
      - marcelloceschia@users.sourceforge.net
